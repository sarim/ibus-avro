imports.searchPath.unshift('..');

const suggestion = imports.suggestionbuilder;

// var suggestionBuilder = new suggestion.SuggestionBuilder();

// print(suggestionBuilder.suggest('sari')['words']);


for (var n in window) {
    print(n);
}

log("gg");